# Validation record

## Automated rules

The core suite covers no-input completion, six automatic ramp jumps, manual jumps, repeated input, bounded steering, loop completion, automatic and manual transformation, pause, invalid frame times, validated saves, every truck threshold, reward limits, and idempotent finish events.

`npm test` passed 34 tests: 10 game-rule tests, 5 audio lifecycle tests, 5 exhaust-flame tests, 7 truck model/resource tests, 2 course-frame tests, and 5 world geometry/visibility tests. The audio tests check user-gesture initialization, the pause-before-first-race flow, immediate mute and voice cleanup, unavailable Web Audio, and graph reuse after pause/resume.

The flame tests cover frozen exhaust and trail matrices while paused, removal of trails with reduced motion, bounded and reused resources over 3,600 driving frames, cleanup on garage/reset/truck changes, and exhaust placement during truck scaling, guardian lift, and loop inversion. Flames share one instanced rendering object with a fixed pool of 64 trail particles.

The model tests exercise all six original designs, articulation and exhaust contracts, finite geometry, and independent/idempotent resource disposal. World tests check the complete transformed trucks at both outside lanes, finite batched geometry and render budgets, loop support clearance, gates above maximum jumping guardian height, and unobstructed support sightlines through the loop. The latter samples 91,584 rays across six trucks, three lanes, and both overview orientations.

## Earlier prototype browser evidence

On 2026-09-11, a headless Chrome run with emulated touch input completed the full 1,900-unit course. The observed result was seven landings (six automatic ramps and one touch jump), one completed loop, 30 collected stars, and 42 awarded stars. The test selected the newly earned Bear Crusher, reloaded to verify the selected truck and earned stars persisted, and began a second race. No JavaScript errors or failed resource responses were recorded in that complete run.

Additional browser interactions covered the garage before the first race, keyboard jumping, pause/resume without distance advancing while paused, motion settings, mute, resizing, and phone controls. Screenshots were inspected at desktop 1440×900, phone 390×844, and iPad-sized 1024×768 viewports, including the transformation and the truck inverted at the top of the loop.

The portable `npm run test:browser` suite passed all three tests in approximately 1.5 minutes: input/pause/resume after a garage visit, a complete race with no driving input followed by unlock/save/replay, and touchscreen play when storage and audio are unavailable. An additional rendering pass started a race with each of the six trucks without browser errors.

## Earlier public deployments

The game was published on 2026-09-11 to [GitHub Pages](https://yanivalfasykeelusa.github.io/james-monster-skyway/) from [its standalone repository](https://github.com/yanivalfasykeelusa/james-monster-skyway). The [initial deployment workflow](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34564169134) completed both build and deployment successfully.

All three portable browser tests then passed against that exact public URL in approximately 1.4 minutes. This verified the repository subpath and bundled assets, keyboard/touch controls, a complete unattended race with the giant loop, earned truck selection, saved progress after reload, replay, and play without storage/audio. The public loop screenshot was inspected. These were headless Chrome tests with emulated touch, not a physical iPad test.

The exhaust-flame update was visually checked locally in Chrome at desktop 1440×900, iPad-sized landscape 1024×768, and portrait 768×1024 layouts. Rumbler and Mega Titan showed scaled flame trails, Mega Titan's jump showed the longer airborne exhaust, and Night Stomper with reduced motion retained two small, steady flames. No browser errors occurred in this visual pass.

All three browser regressions also passed locally with the new flames. The suite now checks active trails, frozen particle counts during pause, automatic guardian flames, exhaust during the loop, and two steady exhaust flames with reduced motion. The captured guardian and inverted-loop frames were visually inspected.

The [flame deployment](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34564806748) successfully published runtime commit `fd489a6` on 2026-09-11. The public site served the expected `index-D09DpkM3.js` bundle, and all three browser regressions passed against its public URL in approximately 1.4 minutes. This included the new flame checks, a complete unattended race, an earned truck, saved-progress reload, and replay. The public inverted-loop screenshot was inspected with the flame trail following the truck.

## Visual quality release: 2026-09-11

The quality pass replaces the primitive truck bodies with six detailed original models, adds a dimensional track and three richer environments, upgrades sunlight/shadows and camera/landing response, and introduces the live lobby, garage portraits, and destination HUD. The race simulation, saved progress format, unlock thresholds, and flame lifecycle remain compatible.

The local production build passed all three browser tests in Chrome (about 1.4 minutes) and Playwright WebKit (about 1.5 minutes). Each engine exercised keyboard/touch jumping, pause/resume, six locally loaded truck portraits, a complete unattended race through every ramp and the inverted loop, automatic transformation and flames, earned-truck selection, saved-progress reload, replay, and play with blocked storage and unavailable audio. The reduced-motion test retained two steady flames. Neither run recorded JavaScript, console, failed-request, or HTTP resource errors.

Independent visual review caught a support that physically cleared the course but briefly hid the ascending truck. A rear gantry and a more direct overview corrected it. Regression coverage checks the actual support geometry, and the finished overview was inspected at ascent, crown, descent and exit in landscape and portrait. A separate review tested 43,200 sightlines through the actual eased camera transitions without a support obstruction. The corrected production build passed the complete Chrome race again in about 1.4 minutes.

An isolated render-lifetime probe switched trucks 42 times across all six designs. GPU geometry counts stayed identical per truck across all seven cycles, with eight textures and ten shader programs in the fixed view. Scene, camera, particles and flame instance matrices remained unchanged through 30 paused updates; an independent review also checked 100 paused updates. These are bounded-resource checks, not physical-device frame-rate measurements.

Desktop 1440×900, tablet landscape 1024×768, tablet portrait 768×1024, and phone 390×844 layouts were captured and inspected. All six portraits loaded, the garage had no horizontal overflow, and essential race controls stayed inside the viewport without overlapping one another. All six selected-truck views and the largest transformed truck in portrait were inspected. The loop browser assertion limits rendering to fewer than 600 draw calls and 400,000 triangles.

The [quality deployment](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34569485051) successfully built and published commit `6b41b88` on 2026-09-11. The public site served the matching `index-pFJ28z9S.js` and `index-B5owj6Dq.css` assets. All three browser tests passed against the public URL in Chrome in about 1.4 minutes, including a complete race, saved unlock and replay. The public loop, guardian and bay screenshots were inspected.

All three tests also passed against that same public URL in Playwright WebKit in about 1.5 minutes. The public runs in both engines recorded no JavaScript, console, failed-request or HTTP resource errors. The WebKit inverted-loop screenshot was inspected. This verifies the deployed repository subpath and browser-engine behavior; physical iPad/Safari testing remains pending.

A separate public-site capture checked the four desktop/tablet/phone layouts, all six garage portraits, and jumping Mega Titan. It found no JavaScript/console errors, horizontal garage overflow, or overlapping/offscreen essential controls. The five README screenshots were refreshed from that public build; the collection capture uses a prepared local browser save to show all six trucks.

## Friendly racing and steering correction: 2026-09-11

This increment adds two trailing racers, position and selected-truck victory displays, trackside festival venues, shallow mud and gentle bay rain. The race rules, automatic driving/jumps/loop, rewards and saved garage remain compatible. The parent reported reversed controls during development; the keyboard Right regression reproduced screen-left movement before the fix. A shared lane-to-world mapping now keeps steering, collectible positions, shadows, follower lanes and mud contact consistent.

The final unit suite passed **52 tests**. New coverage includes six follower tests, three festival geometry/ownership tests, eight weather tests and a chase-camera projection regression through the ordinary course in both tablet orientations. Weather checks include clearance above actual road triangles, contact across all six truck sizes, bounded buffers, pause, reset and gentler motion. The build succeeded; Vite emits an advisory because the approximately 657 kB minified application bundle (173 kB gzip) slightly exceeds its configured 650 kB warning threshold.

The local production Chrome suite passed all five browser tests in about 1.6 minutes. It verifies keyboard and held on-screen steering in both tablet orientations, touch jumping, pause/resume, a full unattended first-place race, follower ordering, automatic transformation and loop, mud spray/rain, selected-truck victory portrait, earned trucks, persistent save/reload/replay, and blocked-storage/unavailable-audio play. The finish awarded 42 stars. Following the full run, visual review reduced the follower scales to keep the player's truck more prominent; additional production touch and layout checks exercised that adjustment.

A separate Chrome probe sent emulated touch-start/touch-end events through both steering buttons at 1440×900, 1024×768, 768×1024, 390×844 and 844×390. Left and right matched the visible road in all five layouts. Position/status and essential controls stayed inside the viewport without overlap. Prepared finish content verified the long multi-truck unlock message and both actions fit all five layouts; real finish logic and portrait loading are covered by the full race test. No JavaScript or console errors occurred.

All five tests also passed on the final local production build in Playwright WebKit in about 1.8 minutes, including the full first-place race, corrected steering, rainy pause/resume and saved replay. Neither browser suite recorded JavaScript, console, failed-request or HTTP errors.

An integrated resource probe switched all six trucks through seven cycles (42 replacements). The fixed bay view retained identical per-truck geometry counts across cycles, seven textures and 13 shader programs. Scene/camera/flame/weather buffers and follower poses stayed unchanged through 30 paused frames. Gentler motion retained two steady exhaust flames and hid moving rain, mud spray and the landing ring. These establish bounded resources and lifecycle behavior, not iPad frame rates.

Independent review checked module behavior and the complete integration. Its visual finding about oversized trailing trucks was addressed by reducing their scales while retaining the safe following gaps. Trackside venues, six player designs, weather and loop views were inspected; the full-field rendering target is fewer than 700 draw calls and 550,000 triangles in tested views.

The review's 13 captures using a moving camera approach recorded no browser errors and a maximum of 530 draw calls and 467,260 triangles. The reviewer closed the player-prominence finding after inspecting all six truck designs and tablet landscape/portrait views.

The [friendly racing deployment](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34581935494) built and published runtime commit `088c7b9` successfully. The public site served the matching `index-DXCmI7i1.js` and `index-Bnv56pNb.css` assets. All five browser tests passed against the public URL in Chrome (about 1.8 minutes) and WebKit (about 1.9 minutes), including corrected steering in both orientations and a complete unattended first-place race with rainy pause/resume, unlock, saved reload and replay. Both runs recorded no JavaScript, console, failed-request or HTTP errors.

A public-site photo pass verified the four desktop/tablet/phone layouts, six loaded garage portraits, no horizontal garage overflow and no overlapping/offscreen essential controls or position/status displays. Public loop, rain/mud, victory, lobby and airborne Mega Titan captures were inspected. README screenshots now show that deployment; the all-trucks collection/lobby captures use a prepared browser save. Physical iPad testing remains a separate follow-up.

## Turbo adventure and toy-car crushing: 2026-09-11

This update adds six crushable parked toy cars, a rechargeable Turbo control, five automatic boost strips, actual friendly overtakes and comebacks, and three coordinated road destinations: Bear Creek Bridge, Rocket Runway and Gator Falls. Saved progress and unlock thresholds remain compatible. Normal crushing awards two stars and charge, briefly slows the truck, then recovers automatically; powered crushing keeps moving.

The final unit suite passed **81 tests**, and the production build succeeded. Coverage includes swept car contact versus airborne/lateral avoidance, one-time rewards, bounded turbo and recharge, automatic pads, truthful rank changes and comeback, all six truck sizes under no-input and repeated-input races, pause and replay. New geometry tests check toy glazing visibility, road-surface placement, flattening/reset/disposal, bridge continuity and actual scenic clearance. Vite reports an advisory for the approximately 676 kB minified application bundle (180 kB gzip), above its configured 650 kB warning threshold.

Independent review found that late steering during an overtake could intersect the player and buddy tire geometry. Predictive yielding, buddy spacing and gentle lateral assistance now preserve room without stopping forward movement or taking rewards. The original reproduction reports zero overlap frames for Rumbler and Mega Titan; 31 independently rerun core/buddy tests pass, including 36 late-merge cases and 24 actual normal/guardian tire-box scenarios across all six trucks. The review closed this finding and reported no outstanding code or visual issues. Clearance also covers expanded guardian wheels and the shrinking animation.

A separate Chrome probe sent emulated touch-start/touch-end events through both steering controls at 1440×900, 1024×768, 768×1024, 390×844 and 844×390. Direction matched the visible road in all five layouts. Turbo, Jump, transformation, steering and the HUD remained in bounds without overlap; prepared finish content and both result actions also fit. No JavaScript or console errors occurred.

An integrated resource probe switched all six trucks through seven cycles (42 replacements). Per-truck geometry counts stayed fixed across cycles, with seven textures and 14 shader programs in the prepared view. Scene, camera, flame/weather buffers, crushed-car poses, boost easing and follower poses remained unchanged for 30 paused updates. Gentler motion retained two steady flames and hid moving rain, mud spray and the landing ring. These check resource lifetime and motion behavior, not physical-device frame rates.

The new encounters add 34 meshes and 9,432 triangles; the three authored destinations add 40 meshes and 16,032 triangles. Independent art review inspected landscape/portrait landmark views, both normal and flattened toy cars, and tablet/phone Turbo layouts. An initial integrated moving-camera pass across 14 views, including transformed Titan, peaked at 596 draw calls and 522,268 triangles; the subsequent ten-view landmark pass after final art adjustments peaked at 549 calls and 499,378 triangles. Both had no browser errors and stayed within the tested 700-call/550,000-triangle budget.

The final local production build passed all **six browser tests in Chrome (about 1.8 minutes) and WebKit (about 2.0 minutes)**. Both verify real crush-to-model flattening, an actual overtake followed by keyboard turbo and regained first place, pause with boost/opponents frozen, correct steering in both tablet orientations, and touch turbo/jump with blocked storage, unavailable audio and gentler motion. Each also completes a full race with no driving input through the new landmarks, automatic boost strips, guardian, loop and rainy bay, then verifies first-place rewards, an earned truck, saved reload and a fresh replay. Neither run recorded JavaScript, console, failed-request or HTTP errors. Real-race bridge, rocket, waterfall, comeback and inverted-loop captures were inspected.

The [turbo adventure deployment](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34626570687) successfully built and published runtime commit `3ed7e76`. The public site serves the matching `index-B5cBZ-hY.js` and `index-L73CJAqG.css` assets.

All **six tests passed against that public URL in Chrome (about 1.7 minutes) and WebKit (about 1.9 minutes)**, including the actual overtake/turbo comeback and a complete unattended first-place race, saved unlock and replay. Both public runs recorded no JavaScript, console, failed-request or HTTP errors. The full-course screenshots and public comeback were inspected. A separate public capture verified four desktop/tablet/phone layouts, all six garage portraits, no horizontal garage overflow and no overlapping/offscreen essential controls. Public airborne Titan, phone controls, tablet menu and phone garage also passed independent visual review. README screenshots now show this deployment, including the timber bridge, rocket runway and waterfall. The collection/lobby captures use a prepared browser save to display the complete garage; physical iPad testing remains pending.

## Smoother world and rewarding crushing: 2026-09-11

The parent requested less pointy graphics, more realism/detail, more natural flames and crushing that is always beneficial. This update replaces the crush slowdown with a one-second 1.18× speed burst, keeping the two-star and 35-charge reward. Stronger 1.55× turbo takes priority without multiplying boosts. Friends approach using their own pace; Sunny can briefly lead, Splash remains behind in ordinary cruising, and an untouched race still finishes first. Six refreshed portraits match the refined bodywork, tires and materials. Rounded branching trees, curved palms, smoother hills and a curved waterfall replace prominent angular silhouettes.

The final **89 unit tests passed**. The new positive-crush regression first failed on the old braking behavior, then passed with the burst. Coverage includes one-time rewards, crushing versus going around a car, power transitions that never reduce speed below cruise, actual rank changes, all-six-truck input patterns and late-merge geometry, paused/replayed state, flame shader/particle lifecycle, independent model resources, normalized organic normals and real vertex clearance. The new crown test found and removed a tree row intruding into the loop footprint. The production build succeeded; Vite retains its advisory for an approximately 686 kB minified application bundle (183 kB gzip), above its 650 kB warning threshold.

A complete controlled browser playthrough used real touch/keyboard controls to crush, turbo, steer both ways, jump, cross the bridge/runway, ride the loop, pause/resume in Gator Bay, finish first, select Bear Crusher and replay. The first crush held first place at speed 30.68, and turbo reached 40.3. The finish recorded six crushed cars, six landings, one loop and 54 earned stars; replay reset the race and preserved the earned truck/progress. Nine gameplay/result captures were taken and the crush, airborne turbo, loop and waterfall were visually inspected. No JavaScript or console errors occurred.

The exhaust uses one instanced volume draw with a fixed 64-particle pool plus two attached jets, at most 66 instances / 792 proxy triangles. Visual review caught the initial fire disappearing when viewed directly from behind the truck. Correct density-sample depth and a tuned powered wake resolved that issue; the final HUD shows soft exhaust beyond the rear tires. Twenty Chrome/WebKit views exercised normal/near/powered/crush/guardian/loop/menu/gentler motion and portrait, with visible flame pixels and frozen shader/matrix/particle state during pause. These establish rendering behavior, not physical-device frame rates.

The final integrated scenery pass inspected sixteen views across both tablet orientations, using 120 moving-camera updates per approach. Maximum submitted geometry was **521,606 triangles**, with maximum **574 draw calls**, within the 550,000 / 700 target. The largest transformed Titan was included at the forest, loop and falls. The adventure factory uses 40 meshes / 20,896 triangles. Near-road crowns and palm fronds have at least 12 units horizontal clearance from the actual sampled road. Better culling and cheaper curved palm geometry offset the extra foreground detail.

An integrated lifetime probe switched all six trucks through seven cycles (42 replacements). GPU geometry counts stayed fixed per truck, with seven textures and 14 shader programs. Scene, camera, particles, flame shader time/instance data, weather, crushed-car poses and followers stayed unchanged through 30 paused updates. Gentler motion kept two steady flames and hid moving rain, mud spray and the landing ring.

Independent rules review and a final independent code/visual review reported no substantive findings. The latter inspected shader depth/lifecycle, model/world geometry, clearance, state transitions, controls and saves, plus final HUD fire, near/gentle/inverted fire, woodland, Night Stomper and transformed Titan at Gator Falls. The brighter material response and softer geometry remain deliberately stylized toy-game art.

The final local production build passed all **six browser tests in Chrome (about 1.8 minutes) and WebKit (about 2.0 minutes)**. Both exercised the positive speed burst and flattened car, paused reward timer, later friendly challenge and manual turbo comeback, touch turbo/jump, correct steering in both tablet orientations, and a complete unattended race through every landmark, guardian, loop and rainy bay. First-place reward, earned-truck selection, persistent reload, clean replay, blocked storage, unavailable audio and gentler motion also passed. Neither suite recorded JavaScript, console, failed-request or HTTP resource errors.

The [smoother world deployment](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34632816929) built and published runtime commit `21c3039` successfully. The public site serves the matching `index-Dbdk0O_j.js` and `index-L73CJAqG.css` assets and the six regenerated truck portraits.

All **six browser tests passed against that public URL in Chrome (about 1.8 minutes) and WebKit (about 2.0 minutes)**. Both engines verified the rewarding crush, actual friendly overtake and turbo comeback, pause/resume, keyboard and touchscreen controls, a complete unattended first-place race, saved unlock and replay, and unavailable storage/audio. Neither public run recorded JavaScript, console, failed-request or HTTP resource errors.

A separate public capture checked desktop 1440×900, tablet landscape 1024×768, tablet portrait 768×1024 and phone 390×844. All six garage portraits loaded, the garage had no horizontal overflow, and essential controls stayed inside each viewport without overlapping. All nine README screenshots now come from this public build. Collection and lobby captures use a prepared browser save to show the complete garage. Public crush, airborne Titan and loop captures were visually inspected; independent review also checked phone driving/garage and WebKit loop/falls views, with no material visual findings. Physical iPad/Safari performance and James's response to this update remain to be observed.

## Playful friends and a living world: 2026-09-11

Sunny and Splash now have separate, deterministic local decision state. They greet at different times, physically imitate successful manual jumps after short delays when the road is clear, and react to crushing and turbo. Sunny favors short races; Splash signals in the two wet stretches and takes a slightly larger following gap there. Completed-race count seeds small timing and color variations without changing the save schema, route, required skill, rewards or safe-passing rules. Original small material tiles add terrain, wood, stone and water detail; fixed roadside habitats add butterflies, cattails and ripples.

The final **113 unit tests passed**, and the production build succeeded. New tests cover successful-action reactions, cooldowns and unsafe-jump suppression, deterministic/serialized continuation, seed normalization, seven distinct event traces and 72 complete seeded races across six trucks and three input patterns. Existing actual-tire guardian/pass checks remain. Renderer tests cover pictograms, camera facing, pause, independent ownership and reset. Audio tests reproduced and corrected an omitted event allowlist entry. Vite retains its advisory: approximately 702 kB minified JavaScript / 188 kB gzip, above the configured 650 kB warning threshold.

A complete production-browser playthrough used the real touch and keyboard controls to trigger greetings, two delayed jump echoes, a car-crush cheer, turbo cheering and a comeback, steering both ways, the bridge/runway/loop, Splash's wet-stretch signal, pause/resume and first-place finish. It awarded 52 stars after five crushed cars, seven landings and one loop; the selected earned Bear Crusher replay preserved progress and used variant 1. Eleven captures were recorded. Independent visual review inspected eight: greeting smiles, jump arrows, star/lightning/drop reactions, unobstructed loop and the clean replay. The second wet-stretch capture had Splash below the frame; the earlier turbo/puddle capture directly shows its drop over the truck. No JavaScript or console errors occurred. The final changes after this playthrough corrected only surface UV projection; final-build browser and scenery checks cover those corrections.

The final scenery probe inspected **24 moving-camera views** across 1024×768, 768×1024 and 390×844, including both friends, transformed Titan at the forest/falls/finish and the loop. Maximum submitted cost was **592 draw calls / 523,142 triangles**, within the 700 / 550,000 target. Ambient life contributes 2,048 triangles across the whole course and uses 14,784 bytes of instance data in 20 spatially culled meshes. Forty life resets retained 869 GPU geometries and 23 textures in the accumulated-view renderer; disposal twice released exactly its five geometries. An independent culling check found all 54,576 sampled animated instance vertices inside declared bounds. These are bounded-resource measurements, not physical iPad frame rates.

A separate integrated lifetime probe replaced the six player trucks through seven cycles. Per-truck geometry counts remained identical through all 42 replacements, with 20 textures and 19 shader programs in the fixed view. Scene, camera, friend poses/signals, world-life time/buffers, flames and weather stayed frozen through 30 paused updates. Gentler motion retained two steady exhaust flames and disabled moving rain, spray and the landing ring; dedicated life/signal tests verify static decorative motion.

Independent review caught blank reaction bubbles caused by opaque material ordering; explicit layer order and smaller bubbles were visually verified. Scenery review caught collapsed waterfall texture coordinates and per-vertex projection seams on wood/stone. Stable projection within each surface triangle or water piece fixed them: an independent check of 83,752 actual textured triangles found no density above 1.01× the intended scale and no collapsed vertical waterfall triangles. Both scoped reviews closed their material findings. Engine and game research, including what was and was not verified, is recorded in [RESEARCH-PLAYFUL-WORLD.md](RESEARCH-PLAYFUL-WORLD.md).

A final-build layout capture checked 1440×900 desktop, 1024×768 tablet landscape, 768×1024 tablet portrait and 390×844 phone viewports. All six portraits loaded; the garage had no horizontal overflow, and essential controls had no overlap or offscreen bounds. Phone driving and airborne Titan were visually inspected. These captures use a prepared browser save to expose the complete garage; they do not alter the game's unlock rules.

After the UV corrections, the final production build passed all **seven browser tests in Chrome (about 1.9 minutes) and WebKit (about 2.3 minutes)**. Each engine verified visible greeting/jump reactions, frozen friends and world life while paused, keyboard/touch steering, jump and turbo, positive crushing and a real comeback, a full unattended first-place race, saved unlock/reload and variant-1 replay, and play with unavailable storage/audio. Both runs recorded no JavaScript, console, failed-request or HTTP errors. Final independent integration review found no further material issues.

The [playful world deployment](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34635802592) successfully built and published runtime commit `fdddcde`. The public page serves the matching `index-DTXNLMLw.js` and `index-L73CJAqG.css` assets.

The public capture repeated all four viewport/garage/control checks without errors, overflow or overlapping/offscreen essential controls. It also used a real touch jump at phone width to capture both physical friend echoes. Independent public visual review confirmed readable phone arrows and controls, airborne Titan exhaust and both WebKit jump pictograms.

All **seven browser tests passed on the public deployment in Chrome (about 2.0 minutes) and WebKit (about 2.4 minutes)**. Both included the new reactions and frozen ambient state, a complete unattended first-place race, saved unlock/reload and the varied replay, plus all existing input, turbo/crushing and fallback checks. No JavaScript, console, failed-request or HTTP resource errors were recorded. The ten README screenshots were captured from this deployment, including the new friends-jumping-together image; the public waterfall/material view was also inspected. Physical iPad/Safari performance and James's response to the new friends remain the next observations.

## Four friends and working landmarks: 2026-09-11 afternoon

The field now includes four friends selected from six original characters. Sunny and Splash remain regulars; completed-race count cycles through all six guest pairings. Each character retains its own preference and timing across slot changes. Reactions have a two-bubble cap and 0.24-second minimum separation. Position and accessible labels describe the five-truck field. A wider chase composition keeps the group visible with the horizon. The bridge waterwheel and woodland windmill now turn around their existing axles; buildings and supports stay fixed.

All **133 unit tests pass**, and the production build succeeds with `index-lgrUEwdq.js` / `index-L73CJAqG.css`. The JavaScript bundle is approximately 715 kB minified / 193 kB gzip; Vite retains its 650 kB advisory. The expanded tests cover 198 complete races across six player sizes, seed/input combinations, and every friend pair, plus 144 late-merge scenarios. Independent review added 648 complete races with mixed frame times and steering, and 360 single-jump runs across 120 seeds: no spacing violations, missing four-friend echoes, unintended leads, pause mutations or finish failures were found. Only Sunny takes a temporary lead in ordinary driving; all tested races finish first.

The six dedicated companion models use 15–16 meshes and 7,668–8,576 triangles each. All were inspected from the front, rear and face-on. Independent articulated-vertex checks keep their geometry inside the required lateral/longitudinal envelope. All 36 ordered roster transitions preserve regular trucks, replace changed guests, recolor reaction bubbles correctly, and dispose retired resources exactly once. The two moving landmarks add five material batches and no triangles. Full-turn vertex sweeps verify road/loop clearance and transformed culling bounds; controller disposal preserves borrowed factory resources.

The final production build passes all **seven browser tests in Chrome (about 2.0 minutes) and WebKit (about 2.4 minutes)**. Both verify touch/keyboard controls, visible staggered jump reactions, pause, positive crushing, a real overtake and turbo comeback, a complete unattended first-place race, earned-truck persistence and a different four-friend replay, plus unavailable storage/audio and gentler motion. No JavaScript, console, failed-request or HTTP errors were recorded. The existing flame lifecycle fixture was updated to include the new landmark controller; the complete unit suite was rerun successfully.

A production capture checks 1440×900, 1024×768, 768×1024 and 390×844 layouts: six garage portraits load, no garage overflow, and no overlapping or offscreen essential controls. Actual touch jumps with Mega Titan and the four-friend phone view were inspected, along with shared jump arrows and the animated bridge composition. The phone naturally crops close roadside landmarks; authored placements are retained for the larger iPad views. These checks do not establish physical iPad frame rate.

The integrated lifetime probe runs 108 roster resets and 108 player replacements over every combination of player and guest pairing. After warm-up, each combination retains identical GPU counts: 385–388 geometries, 20 textures and 21 programs. All 11,117 replaced model resources dispose exactly once; active model resources remain live. Pausing preserves exact transforms for 1,239 scene objects and all 38 dynamic buffers (7,128 values plus versions), camera, effect clocks, friend poses/signals and landmark angles. Six complete course sweeps and refined hotspot samples total 906 measured renders; maxima are **672 calls / 399,738 triangles**, on different frames, within the 700 / 550,000 target. These include the largest guardian, four friends and celebration particles, with no browser errors.

The [crew and landmarks deployment](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34637830657) successfully publishes runtime commit `b8f56d4`. The public page serves the matching `index-lgrUEwdq.js` / `index-L73CJAqG.css` assets. All **seven public browser tests pass in Chrome (about 1.9 minutes) and WebKit (about 2.4 minutes)**, with no JavaScript, console, failed-request or HTTP errors. The public four-viewport capture repeats the garage, control and overflow checks successfully. README gameplay images are refreshed from this deployment; the labeled crew gallery is rendered directly from the same six companion models.

## Lagoon shores and visible crush rewards: 2026-09-11 afternoon

Low, irregular sand banks replace 76 domed lagoon islands. The shared 32-segment shape retains grounded palms and gators, with visible waterline variation and submerged outer edges. Independent review checks 773 actual trunk/foot contact samples against merged bank triangles, road clearance, finite attributes and resource ownership. Static world cost is 604 meshes / 349,592 triangles / 69 materials / 9 textures, saving 608 triangles without adding batches or textures. Three fresh simulation views and the broader 30-view before/after study were visually inspected without browser errors.

Each newly crushed toy car produces two gold stars that rise briefly from the flattened car. This is a visual cue for the existing two-star reward; it does not change rewards or driving. A fixed two-instance mesh adds one draw and 152 triangles while visible, then hides after 0.8 seconds. Unit and independent review cover one cue per actual contact, pause/invalid-frame freeze, replay, gentler motion, culling and exact-once resource disposal. Forty rendered samples cover two player sizes, forest/bay cars and tablet/phone views; the stars remain readable behind Mega Titan. Twelve independently simulated full races confirm unchanged awards.

All **141 release unit tests pass**. The production build produces `index-DIABTUBB.js` / `index-L73CJAqG.css`, approximately 719 kB minified / 194 kB gzip JavaScript, with the existing Vite size advisory. All **eight production browser checks pass in Chrome (about 2.1 minutes) and WebKit (about 2.6 minutes)**. Added coverage checks visible crush stars and their frozen/expired/reset states, plus rotating the tablet during driving and while paused without losing the race or usable touch controls. Both complete races finish first, save an earned truck and replay successfully; neither engine records browser/network errors.

The [shore and crush-star deployment](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34639371761) successfully publishes runtime commit `aab8ec0`; the public page serves `index-DIABTUBB.js` / `index-L73CJAqG.css`. All **eight public browser checks pass in Chrome (about 2.2 minutes) and WebKit (about 2.8 minutes)**, including complete first-place races, orientation changes, crush-star pause/replay and persistent unlocks. A public four-viewport capture verifies loaded portraits and no overflowing garage or overlapping/offscreen controls. No browser/network errors were recorded. Public screenshots show the current reward cue and shoreline.

## Distinct truck bodies, softer forest and batched celebrations: 2026-09-11 afternoon

The garage grows to eight vehicles with Rescue Roarer at 40 stars and Shark Surge at 48. Their complete fire-engine and shark bodies replace the generic pickup shell; Mega Titan has a narrower lifted body, exposed rear coilovers and the existing largest tire footprint. The other five models' geometry/material attributes match the prior release exactly. Existing IDs, thresholds and version-1 saves are retained. Independent review verifies 36 old-save round trips and 24 additional all-eight-truck races; all finish first with positive rewards. The menu badge now derives its total from the catalog. Four columns show all eight garage actions at 1024/1440 landscape widths; portrait retains two columns. Twelve layout captures show loaded portraits, readable names and no horizontal overflow.

All **157 unit tests pass**, including the expanded models, eight-truck clearance/passing and save checks. Model-specific review tests finite physical materials, 216 articulation envelopes, equipment/head clearance, exact tire-buffer preservation and independent resource disposal. The final portraits are rendered from the actual models. Nearby forest crowns use the accepted 20×12 shape after a matched 54-view comparison; distant trees remain simpler. Static world cost is 377,240 triangles / 1,049,207 vertices with the existing 604 mesh batches, 69 materials and 9 textures.

Celebration particles now share one 80-instance mesh. Independent paired WebGL checks match all 32 sampled legacy particle/camera/race states, with the documented gold-flake metalness change. A full pool saves **79 draw calls** with identical geometry. Review caught and fixed stale suppression after leaving gentler motion and an automatic guardian sparkle emission during a defensive positive-dt paused call. Exact paused buffers, menu/reset/truck replacement, gentler clearing, immediate preference exit and moving finish confetti are verified. New guardian sparkles stop after finishing; the main finish celebration still animates and expires.

The integrated resource probe completes **144 roster resets and player replacements** over all 48 player/guest combinations. Retired model resources dispose exactly once and warmed resource counts remain identical per combination. Pausing freezes all 1,165 scene transforms and 41 dynamic buffers (8,680 values plus versions), camera, clocks, particles, friends and landmarks. Six complete course sweeps render 498 measured frames, peaking at **624 calls / 429,173 triangles**, including the largest guardian and full celebration bursts; no browser errors occur. These measurements remain within the 700-call / 550,000-triangle target and do not establish physical iPad frame rates.

Independent model integration review finds no remaining issues: 960 articulated vertex poses, 51,200 actual steering frames, 11,544 loop-envelope poses and 4,016 resource-disposal checks pass. All eight tire geometries match the prior baseline. Twenty-four actual GameScene views cover the three new/changed forms at tablet/phone widths, ordinary and automatic guardian states, and front/rear detail; the ladder, hose reel, shark tail/fins and Titan springs stay readable.

The production build succeeds with `index-DNvN4krW.js` / `index-BDydzDdE.css`, approximately 727 kB minified / 197 kB gzip JavaScript. The existing Vite 650 kB size advisory remains.

All **ten production browser checks pass in Chrome (about 2.4 minutes) and WebKit (about 3.2 minutes)**. Added checks select each new truck at its earned balance, reload to verify the saved ID, then use touch to jump and crush a toy car. The complete guided race, four-friend reactions, orientation/pause, turbo/comeback and storage/audio fallback checks also pass, with no browser/network errors. A production capture repeats four viewport/portrait/control checks successfully. A clean rebuild after final source cleanup produces the same asset hashes.

A separate three-race production playthrough starts with an empty save and earns 54, 108 and 162 total stars. Rumbler, Rescue Roarer and Shark Surge each finish first, crush six cars and complete the loop. Rescue Roarer completes the entire race without driving input; Shark Surge runs at 768×1024 portrait. Earned selections survive reload, and the final garage allows the earned Mega Titan selection. Pause/resume and both new trucks' course views were exercised with zero browser, console or HTTP errors.

The [distinct-trucks deployment](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34643522801) successfully publishes runtime commit `80aeecb`; the public page serves the matching `index-DNvN4krW.js` / `index-BDydzDdE.css` assets. All **ten public browser checks pass in Chrome (about 2.6 minutes) and WebKit (about 3.5 minutes)**. The public four-viewport capture loads all eight portraits with no garage overflow or overlapping/offscreen essential controls. No browser/network errors were recorded. Independent source review found no additional controls, save, progression or documentation issue. README gameplay screenshots are refreshed from this deployment.

## Rounded horizon and working rear coils: 2026-09-11 afternoon

The 38 distant hills use the independently reviewed 24×14 contour, retaining their placements and seven cullable groups. An 18-frame comparison preserves matched race/friend/camera states. Static world geometry becomes 393,504 triangles / 1,097,999 vertices with the same 604 batches, 69 materials and nine textures. Independent complete hill bounds leave ample road clearance; [the design](DESIGN-SOFT-HORIZON.md) records the comparison and its sampled-path limits.

Titan's three existing rear-suspension batches now shorten with the existing landing dip. Body, wheel, arm and camera poses remain unchanged, as do the other seven trucks. This stateless borrowed pose adds no GPU resources or motion timer. A 28-pair study shows a small four-pixel shortening at landing, with unchanged outer bounds and render cost. The stronger body-bounce candidate was rejected for internal tire overlap. [Working-coils design](DESIGN-WORKING-COILS.md) records the accepted narrower implementation.

All **162 final unit tests pass**, including fixed/following mount checks, invalid-input clamping, unchanged other-model transforms and resource ownership. The real scene lifecycle fixture checks immediate neutral springs on reset, menu and truck replacement. A six-course rendering sweep dispatches actual land/crush events and measures 498 frames at tablet/phone widths, peaking at **625 calls / 437,269 triangles**. Every course finishes and all four friends remain active, with no browser errors. This sweep preceded the final steering taper, which adds no geometry or resources. The final production build produces `index-QZDzUopr.js` / `index-BDydzDdE.css`, approximately 728 kB minified / 197 kB gzip JavaScript, with the existing Vite size advisory.

Independent integration review verifies unchanged outer bounds and non-spring transforms across 1,440 articulated poses. A broader real-input replay caught two rapid accessible-steering cases with added spring/tire contact; the final helper fades compression from full at 0.14 radians of body lean to neutral at 0.18. Repeating **192 complete race traces / 409,896 simulated frame updates / 9,696 precise landing poses** with the final helper finds zero added contacts. The two reported cases return to their baseline geometry. This covers six variants, held and accessible steering, four frame-time patterns, automatic crushing, transformation and the loop. A final actual-scene check confirms exact pause and neutral menu/reset/replacement/loop/gentler poses, with no browser errors.

After the steering correction, all **ten final production browser checks pass in Chrome (about 2.4 minutes) and WebKit (about 3.1 minutes)** against the rebuilt `index-QZDzUopr.js` bundle. They cover the full guided race, keyboard/touch controls, pause/orientation, friends' reactions, positive crushing/turbo, both new saved selections, and unavailable storage/audio. Neither engine records browser/network errors. The earlier pre-taper browser runs also passed, but the final runs are the release evidence.

The [horizon and coils deployment](https://github.com/yanivalfasykeelusa/james-monster-skyway/actions/runs/34645630952) successfully publishes runtime commit `c5b6528`. The public page serves the matching `index-QZDzUopr.js` / `index-BDydzDdE.css` assets. All **ten final public browser checks pass in Chrome (about 2.5 minutes) and WebKit (about 3.1 minutes)**, with no JavaScript, console, failed-request or HTTP errors.

The first public Chrome run passed nine checks; the full-race assertions also passed, but its network guard reported six portrait downloads cancelled during the test's deliberate reload. The trace shows HTTP 200 response headers followed by `ERR_ABORTED` in that navigation window. The harness now waits for all eight complete, 720-pixel portraits both before selection and after the garage cards are rebuilt, before reloading. Independent review verified the diagnosis and targeted synchronization. Strict network-error collection remains unchanged; both complete public suites then pass with the stronger image check. No application change was needed.

A separate controlled public playthrough selects Mega Titan from a prepared 110-star save and verifies selection through reload, touch jumps, both steering directions, pause/resume, turbo, automatic guardian activation and the loop. Gentler motion retains two steady exhaust flames and playable jumps; turning it off restores the ordinary effects. Titan finishes first with eight landings, one loop and six crushed cars: 42 collected race stars plus the 12-star finish award raise the saved balance to 164. A final reload preserves that balance and Titan selection. No JavaScript, console or HTTP errors occurred. The prepared starting save exposes the final truck; the earlier three-race progression run separately verifies earning the garage from empty storage.

Final public captures repeat the 1440×900, 1024×768, 768×1024 and 390×844 layout checks. All eight portraits load, the garage has no horizontal overflow, and essential controls remain on screen without overlapping. Independent final visual review finds no actionable issue in Titan's landing, gentler-motion jump and bay views, phone driving, or the eight-truck garage. README gameplay, menu and garage screenshots now show this deployment; collection captures use a prepared save. Physical iPad/Safari performance remains unverified.

## Robot flight, selectable tracks and controller input: 2026-09-11 evening

Five immediately available races now use their own start, finish and progress bounds: Skyway Adventure, Bear Woods, Sky Loop, Gator Bay and Canyon Run. The robot can be selected from the start; Fly follows a guided distance-based arc and lands safely. Four actual gaps split the canyon road and rails, with launch/landing assistance for the player and all four friends. Fifteen authored toy targets are filtered by the chosen course and flight corridors; each successful smash gives three stars, turbo charge and a short speed burst once. Version-1 saves, all eight truck IDs and unlock thresholds, and the existing capped finish award remain compatible.

All **206 unit tests pass**. Rules checks cover every course, all eight truck sizes, no-input first-place finishes, turbo, held/repeated actions, pause, old saves, replay, loop transitions and safe canyon flights. Independent geometry review checks actual absent road triangles, 112 actor/gap/edge cases and 15,794 posed-model/lip-plane clearance samples. Minimum clearance above the highest road rail is 5.4166 units. Short-course finish portals clear complete flying models, including their forward overhang. Existing full-course gates were raised after a real flight/overhead intersection was found; regressions include a jump carried into a ramp and all eight robots. Robot tire contact now earns the reward during robot form and the visible return to truck; 32 actual car/block contact poses plus separated/airborne misses are checked.

The eight parked truck designs remain unchanged. The articulated robot adds a helmet, armored legs and boots, fists, short wings and jet details. Actual posed bounds, exhaust attachment, finite geometry and independent resource disposal are tested. The canyon uses cached, spatially batched scenery with owned resources; smash parts have fixed geometry and only respond to simulation hits. Small toy braces and barrel rings receive lighting while the main bodies cast shadows, avoiding unnecessary tiny shadow passes.

An integrated scene review completes 120 truck replacements and 120 course swaps. All 14,186 retired model resources dispose exactly once; all 236 cached canyon resources remain stable. Matching warmed states retain identical GPU counts. Pause freezes 1,612 objects and 41 dynamic buffers, including camera, friends, effects and smashed fragments. Six full Skyway/Canyon races across tablet and phone sizes produce 642 measured renders. A dense six-roster early-flight follow-up finds the final peak at **691 draw calls / 456,328 triangles**, within the existing 700 / 550,000 target. These are sampled rendering counts, not physical iPad frame rates.

Actual flight screenshots and projected head bounds exposed a label covering the robot's helmet. The compact callout now sits below the HUD, to the left in landscape and centered in portrait. Checks include the highest fourth-gap takeoff, with at least 140 pixels of horizontal head/banner separation in landscape and 14.99 pixels of vertical separation at phone width.

Controller tests replace navigator.getGamepads with controlled standard snapshots while exercising the real sampler, DOM adapter and animation loop. Checks cover analog deadzones, D-pad override, action edges, turbo, flight, exact pause, reconnect and active-controller replacement. Review caught and corrected lost picker-return focus, unreachable home Sound/Settings, unmuted initial focus, and Menu accidentally confirming a focused reset button. A saved 23-star/two-race fixture verifies that Menu closes reset confirmation without clearing progress. Physical controller hardware is not injected or emulated by these tests.

Touch regression uses real Chromium multi-contact events for simultaneous steering/jumping, partial finger release, cancellation and pinch movement. Rapid taps leave viewport scale unchanged; text selection stays empty; single-finger garage scrolling remains available. WebKit separately verifies the Safari gesture-cancellation fallback, selection styles, rapid taps and browser scrolling. Chromium's multi-contact case is explicitly skipped in WebKit because that runner has no equivalent multi-contact input API. Neither engine proves physical iPad behavior.

Four viewport captures at 1440×900, 1024×768, 768×1024 and 390×844 show loaded garage portraits, readable picture track choices, giant robot flight and usable controls without overlap or offscreen buttons. They also rotate the viewport while paused and complete the first canyon landing after resuming. Prepared 110-star saves expose Mega Titan for these visual checks; ordinary progression and saved rewards are exercised separately by the full-race tests.

The production baseline passes **26 browser checks in Chrome and 25 in WebKit**, with the documented Chromium-only multi-contact case skipped in WebKit. These include complete first-place races on all five tracks, four canyon crossings, earned-truck persistence and replay, positive smashing, the existing friendly-racer challenge and storage/audio fallback. After the final garage-outline and callout corrections, the rebuilt release passes all **14 affected checks in Chrome and 13 in WebKit**, with the same explicit skip. The added ninth controller case chooses Bear Crusher through the garage, checks focus on its newly rendered card, returns to Play and verifies the saved choice. Both engines retain strict JavaScript, console, failed-request and HTTP-error guards with no errors. The final four-viewport capture also passes.

The final build produces `index-DZFPZm6w.js` / `index-BEuZ393k.css`, approximately 760 kB minified / 209 kB gzip JavaScript. The existing Vite 650 kB bundle-size advisory remains; runtime dependencies are unchanged. Public deployment verification follows below when completed.

## Practical limits

- Chrome and Playwright WebKit on a desktop with emulated touch are not a physical iPad or iPhone. Shipping Safari behavior, actual device frame pacing, battery use, and orientation changes still need a real-device pass.
- James's parent has shared enthusiastic feedback; the updated controls, course and unlock pacing still need focused observation on his actual play device.
- Local saves belong to a browser and site origin; moving between preview and public URLs creates separate garages.
- Storage is not cloud-synced. Private browsing and storage clearing can remove progress.
- GitHub Pages publication has been verified; future deployments should repeat the public URL regression suite.
